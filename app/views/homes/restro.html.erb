<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Bootstrap 101 Template</title>

    <!-- Bootstrap -->
    <!-- <link href="css/bootstrap.min.css" rel="stylesheet">
 -->
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  <style>
  
.modal-body {
    max-height: calc(100vh - 160px);
    overflow-y: auto;
}
.modal-dialog {
      width: 800px; /* New width for default modal */
        }
  </style>
  
  </head>
  <body>



  <div class="container-fluid height-full" style="background-color:white;border-top-color:black;border-style:solid;border-right-color:black;border-left-color:black;border-width:1px;border-bottom:none;">

      <br><br>
      <%@products.each.with_index(1) do |product, index|%>
      <%if (index-1)  % 3== 0%>
        <div class="row col-lg-offset-2">
      <%end%>    
        <div class="col-lg-3 col-md-3" onmouseover="LaunchModelMosueOver('p<%=index%>')" onmouseout="LaunchModelMosueOut('p<%=index%>')" id="product1">
          <%=image_tag product.images.last.name, class: "img-responsive" %>
          <!--<a href="#itemDescription" class="btn btn-primary btn-lg" type="button" style="height:100%;width:100%" id="p<%=index%>" data-toggle="modal">Launch demo modal</a>-->
          <%=render 'popup', product: product%>
          <%= link_to "Launch demo modal", '#', data: { toggle: "modal", target: "#itemDescription_#{product.id}" }, class: "btn btn-primary btn-lg", id: "p#{index}",  style: "height:100%;width:100%" %>
          <h4><%=product.name%> <span class="label label-default"><%=product.price%></span></h4>
        </div>
        <%if index  % 3 == 0%>
          </div>
        <%end%> 
      <%end%>
  </div>
 <%#=render 'popup'%>

  
  
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
     <!-- <script src="js/bootstrap.min.js"></script> -->
  <script>
  $(document).ready(function(){
    $("#p1").hide();
    $("#p2").hide();
    $("#p3").hide();
    $("#p4").hide();
    $("#p5").hide();
    $("#p6").hide();
    $("#p7").hide();
    $("#p8").hide();
    $("#p9").hide();
    });
    function LaunchModelMosueOver(id)
    {
    var mid='#'+id
    $(mid).show();
    }

    function LaunchModelMosueOut(id)
    {
    var mid='#'+id
    $(mid).show();
    $(mid).hide();
    }
  </script>
  </body>
</html>